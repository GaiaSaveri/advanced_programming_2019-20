#include "constants.h"
#include <iostream>

double golden_ratio(const double a) {
  std::cout << "[greek] pi = " << pi << std::endl;
  return a / phi;
}

//not an header --> how to compile?
//need to wright the signature in the main
